
spring:

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lenscraft
    username: postgres
    password: srjmgrz786

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format-sql: true

    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  output:
    ansi:
      enabled: ALWAYS

  mail:
    host: smtp.gmail.com
    port: 587
    username: team.lenscraft@gmail.com
    password: fivo zrkw jkfw usty
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enabled: true
            required: true
